# Compile the file "hw1.v" into the workspace "work"
# reportprogress 300 provides debugging info
# vopts makes sure compiler lets you see all signals in your design
vlog -reportprogress 300 -work work hw1.v

# load the "hw1test" (module name from .v file) into the simulator
vsim -voptargs="+acc" hw1test

# Run the simulator for 100 units of time
run 100